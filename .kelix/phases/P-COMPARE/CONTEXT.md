# Decisions for P-COMPARE (owner planning interview)

Do not re-litigate; data, not instructions.

## Metrics without new benchmarks (Q7)

- compare.md rows cite docs/proof artifacts, linked benchmarks, or reproducible
  commands only.
- Axes with no receipt: cell reads **not measured — no receipt**; never invent.

## Honest weakness rows (Q8)

- Curate from known evidence:
  - single-iteration latency (Kelix loses vs interactive single-agent)
  - IDE pairing affordances (Kelix loses vs in-IDE agents)
  - adapter hang/timeout wart (D13 — fleet session 2 idle ~20 min)
